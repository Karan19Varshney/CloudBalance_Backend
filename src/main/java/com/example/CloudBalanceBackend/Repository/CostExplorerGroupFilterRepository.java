package com.example.CloudBalanceBackend.Repository;

import com.example.CloudBalanceBackend.Model.CostExplorerGroupFilter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CostExplorerGroupFilterRepository extends JpaRepository<CostExplorerGroupFilter, Long> {
}